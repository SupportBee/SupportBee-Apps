checkout:
  post:
    - cp honeybadger.yml.example honeybadger.yml
    - cp omniauth.platform.yml omniauth.platform.example.yml
    - cp sba_config.yml.example sba_config.yml

machine:
  ruby:
    version: 1.9.3-p484
  environment:
    RAILS_ENV: test

database:
  post:
    - cp config/database-ci.yml config/database.yml
