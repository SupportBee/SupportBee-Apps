<h3 class="ui dividing header">Send to Bugify<span class="hint">Edit Task Details</span></h3>
<form class="ui app form">
  {{#each tickets}}
    {{#ticket}}
      <div class="field">
        <label for="project">Select Project</label>
        <select name="projects_select"></select>
      </div>
      <div class="title field">
        <label for="title">Title</label>
        <input name="title" type="text" value="{{subject}}">
      </div>
      <div class="description field">
        <label for="description">Description</label>
        <textarea name="description">{{content.text}}

https://{{../../company.subdomain}}.supportbee.com/tickets/{{id}}
        </textarea>
      </div>
      <div class="assign field" style="display:none;">
        <li >
          <label for="assign_to">Assign To</label>
          <select name="assign_to">
            <option value="none">Don't Assign</option>
          </select>
        </div>
      </div>
      <div class="actions">
        <a class="ui submit primary button btn">Send to Bugify</a>
        <a class="ui right floated cancel basic button btn">Cancel</a>
      </div>
    {{/ticket}}
  {{/each}}
</form>
